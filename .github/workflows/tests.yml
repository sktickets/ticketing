name: Tests

on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/ckeckout@v2
        with:
          token: ${{ secrets.PAT_ACTIONS }}
          repository: sktickets/ticketing
          path: ticketing
      - run: cd auth && npm i && npm run test:ci
